# SQLite 本地运行示例（无需 MySQL）
# 复制本文件为 .env 后直接 uv run uvicorn app.main:app --reload 即可
# 数据库文件自动创建在 backend/ 目录下

DATABASE_URL=sqlite+aiosqlite:///./fancy_agent.db
OSS_URL=http://localhost:8000
SECRET_KEY=local-dev-secret-key
# 敏感字段（LLM/图片工具 api_key）落库加密密钥，可选；留空则复用 SECRET_KEY 派生
# APP_ENCRYPTION_KEY=
UPLOAD_DIR=./data/uploads
WORKSPACE_DIR=./data/workspaces

# 邮件助手（可选，默认关闭）。开启后用单个共享邮箱收发信，按发件人匹配注册用户。
# EMAIL_PROVIDER 预设：gmail / 163 / qq / outlook，自动带出 IMAP/SMTP 主机端口加密方式。
# Gmail 需用「应用专用密码」（开启两步验证后生成的 16 位密码），不能用登录密码。
# EMAIL_ENABLED=true
# EMAIL_PROVIDER=gmail
# EMAIL_ADDRESS=you@gmail.com
# EMAIL_USERNAME=you@gmail.com
# EMAIL_PASSWORD=你的16位应用专用密码
# 自定义服务商可显式覆盖：EMAIL_IMAP_HOST/PORT、EMAIL_SMTP_HOST/PORT、
# EMAIL_SMTP_SECURITY=ssl|starttls、EMAIL_IMAP_ID=true（163/QQ 收信必需）

# MLflow 可观测性追踪（默认关闭，启用需先安装：cd backend && uv sync --extra observability）
# 开启后自动追踪所有 LangChain/LangGraph 调用（LLM 调用、工具调用、token、延迟）
# MLFLOW_ENABLED=true
# MLFLOW_EXPERIMENT_NAME=fancy_agent_dev
# 本地文件存储（无需任何服务器）。留空会默认在 backend/ 下生成 mlruns/、mlartifacts/，
# 建议显式指向 data/ 目录，与 uploads/workspaces 统一管理、便于 gitignore：
# MLFLOW_TRACKING_URI=file:./data/mlruns
# 集中/MySQL 存储：单独跑一个独立的 MLflow Server（自带 pymysql，与主应用环境隔离），
# 应用侧仅通过 HTTP 连接，主应用环境无需安装 pymysql：
#   mlflow server --backend-store-uri mysql+pymysql://user:password@host:3306/mlflow \
#                 --artifacts-destination /path/to/mlartifacts
# MLFLOW_TRACKING_URI=http://your-mlflow-server:5000
#
# 注意：MLflow 始终是「双存储」。--backend-store-uri 只存结构化元数据
# （experiment / run / params / metrics / trace span，适合 MySQL）；真正的工件文件
# （模型、图片、traces.json 等大文件）落在 --artifacts-destination 指向的 artifact store。
# 即便后端是 MySQL，artifact 也仍在本地目录/对象存储，不在数据库里 ——
# 误删 artifact 目录只丢历史工件文件，UI 的 run 列表与数值仍在数据库中完好。

# ── 存储后端（可选，默认 local）──────────────────────────────────────────────
# 切换到 S3 兼容存储（阿里云 OSS / AWS S3 / MinIO）：取消注释并填写凭据。
# STORAGE_BACKEND=s3
# S3_ENDPOINT_URL=https://oss-cn-hangzhou.aliyuncs.com   # OSS S3 兼容端点；AWS S3 留空
# S3_ACCESS_KEY_ID=your-access-key-id
# S3_SECRET_ACCESS_KEY=your-access-key-secret
# S3_BUCKET=your-bucket-name
# S3_REGION=cn-hangzhou
# OSS_URL=https://your-bucket.oss-cn-hangzhou.aliyuncs.com
#
# S3 访问 URL 模式（默认 public）：
#   public    — bucket 设为 public-read，返回明文永久 URL（任何人拿到链接即可访问）。
#   presigned — bucket 保持私有，后端生成带签名 + 过期时间的临时 URL（最安全）。
#               OSS_URL 仍填 bucket 域名；URL 会过期，聊天历史里的图片在返回前端时自动重签。
# S3_URL_MODE=presigned
# S3_PRESIGN_EXPIRE=3600   # 预签名有效期（秒），默认 3600=1 小时
